{
  "name": "@lintx/chrome-extension",
  "version": "1.0.0",
  "description": "Chrome extension to view protobuf files on GitHub",
  "type": "module",
  "scripts": {
    "prebuild": "npm run generate-config-schema",
    "build": "webpack --mode production",
    "dev": "webpack --mode development --watch",
    "clean": "rm -rf dist",
    "generate-config-schema": "node scripts/generate-config-schema.js"
  },
  "dependencies": {
    "@lintx/core": "workspace:*"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.268",
    "@types/node": "^20.11.0",
    "copy-webpack-plugin": "^12.0.2",
    "ts-loader": "^9.5.1",
    "typescript": "^5.4.0",
    "webpack": "^5.90.0",
    "webpack-cli": "^5.1.4"
  }
}
