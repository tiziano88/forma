{
  "name": "forma-monorepo",
  "private": true,
  "scripts": {
    "install:all": "pnpm install",
    "build": "pnpm -r build",
    "clean": "pnpm -r clean",
    "dev": "pnpm --filter shared-ui build --watch & pnpm --filter web-app dev",
    "format": "prettier --write .",
    "forma": "node cli/dist/index.js"
  },
  "devDependencies": {
    "fs-extra": "^11.1.1",
    "prettier": "^3.3.2",
    "prettier-plugin-svelte": "^3.2.5",
    "typescript": "^5.4.0"
  },
  "dependencies": {
    "ts-protoc-gen": "^0.15.0"
  }
}
